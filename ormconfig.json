{
    "entities": [
        "dist/**/*.entity{.ts,.js}",
        "src/**/*.entity{.ts,.js}"
    ],
    "timezone": "Z",
    "synchronize": true,
    "seeds": [
        "dist/**/*.seed{.ts,.js}",
        "src/**/*.seed{.ts,.js}"
    ],
    "factories": ["dist/**/*.factory{.ts,.js}"]
}